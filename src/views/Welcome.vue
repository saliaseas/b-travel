<template>
  <div class="welcome container">
    <p>Welcome</p>
    <div v-if="showForm">
      <h2>Login</h2>
      <Login @talgat="handleSubmit" />
      <p>
        No account yet? <span @click="showForm = false">Sign Up</span> instead.
      </p>
    </div>
    <div v-else>
      <h2>Sign Up</h2>
      <SignUpForm @islam="handleSubmit" />
      <p>
        Sign up now. <span @click="showForm = true">Login</span> instead.
      </p>
    </div>
  </div>
</template>

<script>
import SignUpForm from "@/components/SignUpForm.vue";
import Login from "@/components/Login.vue";
import { ref } from "@vue/reactivity";
import { useRouter } from "vue-router";
export default {
  components: { SignUpForm, Login },
  setup() {
    const showForm = ref(false);
    const router = useRouter();

    const handleSubmit = () => {
      router.push("./chatroom");
    };
    return {
      showForm,
      handleSubmit,
    };
  },
};
</script>

<style>
.welcome {
  text-align: center;
  padding: 20px 0;
}
.welcome form {
  width: 300px;
  margin: 20px auto;
}
.welcome label {
  display: block;
  margin: 20px 0 10px;
}
.welcome input {
  width: 100%;
  padding: 10px;
  border-radius: 20px;
  border: 1px solid #eee;
  outline: none;
  color: #999;
  margin: 10px auto;
}
.welcome span {
  font-weight: bold;
  text-decoration: underline;
  cursor: pointer;
}
.welcome button {
  width: 503px;
  height: 75px;
  background: #990119;
  border-radius: 20px;
}
</style>
